{
  "scripts": {
    "build:chat": "npm --prefix web run build",
    "dev:chat": "npm --prefix web run dev",
    "generate:backlog": "sh scripts/run_python.sh data/generate_backlog_recommendations.py --force",
    "start:dashboard": "sh scripts/run_python.sh data/generate_backlog_recommendations.py --allow-stale-on-error && npm run build:chat && sh scripts/run_python.sh -m backend.reviewlens_server"
  },
  "devDependencies": {
    "prettier": "3.8.4"
  }
}
